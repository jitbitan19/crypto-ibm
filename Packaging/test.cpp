#include <bits/stdc++.h>
using namespace std;

int main()
{
    int d = 0, a = 10;
    if (d != 0 && a % d == 0)
        cout << "d is a factor of a" << '\n';
    else
        cout << "not working" << '\n';
    return 0;
}